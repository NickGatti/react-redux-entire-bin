import getMessage from './actions'

const initialState = [

]

export const getMessageReducer = ( store = initialState, action) => {
    switch (action.type) {
        default:
            return store
    }
}